pre-commit:
  parallel: true
  commands:
    lint:
      run: bunx biome lint {all_files} --fix
    format:
      run: bunx biome format {staged_files} --fix --no-errors-on-unmatched
    tests:
      run: cd apps/api && bun test {all_files}
